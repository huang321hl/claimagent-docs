---
title: "访问凭证"
description: "API Key 鉴权与安全建议"
---

## 鉴权方式

所有接口均需要 API Key 认证。请求时在 Header 中携带 API Key：

`x-api-key: {API_KEY}`

## 使用建议

*   **API Key 视同密码**，避免在前端代码、日志、截图中泄露
*   **建议为不同环境（测试/预发/生产）使用不同的 API Key**，便于权限隔离与问题排查
*   **异步回调 `callback_api` 仅在任务完成后触发**，建议回调接口自行校验来源与请求签名/白名单（如有）
